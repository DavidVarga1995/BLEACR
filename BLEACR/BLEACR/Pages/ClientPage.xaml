<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BLEACR.Client"
             x:Class="BLEACR.Pages.ClientPage"
             Title="Search">
    <ContentPage.Content>
        <StackLayout x:Name="ClientContent">
            <Label x:Name="WriteReceived" Text="Change between Bluetooth Low Energy Client and Server:"/>
            <Button x:Name="Client" Text="Client" Clicked="OnClientButtonClicked" CornerRadius="30"/>
            <Button x:Name="Server" Text="Server" Clicked="OnServerButtonClicked" CornerRadius="30"/>
            <!--<Label x:Name="Test" Text="TEST" />-->
            <ListView x:Name="listView" x:FieldModifier="public" SeparatorVisibility="None" 
                  RowHeight="60"
                  ItemsSource="{Binding Path=local:SetUpClient.devices}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Name}"/>
                                <Label Text="{Binding Uuid}"/>
                                <Label Text="{Binding Rssi}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>